name: MS-GameHUD BUILD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  PROJECT_PATH: "MS-GameHUD.sln"
  PROJECT_NAME: "MS-GameHUD"
  OUTPUT_PATH: "./build"

jobs:
  build:
    permissions: write-all
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v6
    - name: Setup .NET
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: 10.0.x
    - name: Restore
      run: dotnet restore
    - name: Build
      run: dotnet build ${{ env.PROJECT_PATH }} -c Release
  publish:
    if: github.event_name == 'push'
    permissions: write-all
    runs-on: windows-latest
    needs: build
    steps:
    - uses: actions/checkout@v6
    - name: Setup .NET
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: 10.0.x
    - name: Restore
      run: dotnet restore
    - name: Build
      run: dotnet build ${{ env.PROJECT_PATH }} -c Release
    - name: Set build version
      run: |
        $VER = Get-Content VERSION
        echo ("BUILD_NUMBER=$VER") >> $env:GITHUB_ENV
    - name: Zip
      run: |
        cd ${{ env.OUTPUT_PATH }}
        Compress-Archive -Path * -Destination "../${{ env.PROJECT_NAME }}-v.${{ env.BUILD_NUMBER }}.zip"
    - name: Publish plugin
      uses: ncipollo/release-action@v1.21.0
      with:
        artifacts: "${{ env.PROJECT_NAME }}-v.${{ env.BUILD_NUMBER }}.zip"
        name: "${{ env.PROJECT_NAME }}-v.${{ env.BUILD_NUMBER }}"
        tag: "${{ env.PROJECT_NAME }}-v.${{ env.BUILD_NUMBER }}"
        generateReleaseNotes: true
        makeLatest: true
        body: |
          [Core]${{ env.PROJECT_NAME }} release ${{ env.BUILD_NUMBER }}. API for displaying messages to the player. GameText analogue (using point_worldtext)
